{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-info float-right" href="{{ path('blog_index') }}">Back to the list</a>
    <a class="btn btn-info float-right" href="{{ path('blog_edit', { 'id': blog.id }) }}">Edit</a>
    <h1>Blog</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ blog.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ blog.content }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{% if blog.createdAt %}{{ blog.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Updatedat</th>
                <td>{% if blog.updatedAt %}{{ blog.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ blog.image }}</td>
            </tr>
        </tbody>
    </table>
            {{ form_start(delete_form) }}
                <input class="btn btn-info" type="submit" value="Delete">
            {{ form_end(delete_form) }}

{% endblock %}
