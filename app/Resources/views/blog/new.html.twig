{% extends 'base.html.twig' %}

{% block body %}
    <h1>Blog creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-success" />
        <a href="{{ path('blog_index') }}" class="btn btn-info">Back to the list</a>
    {{ form_end(form) }}


{% endblock %}
